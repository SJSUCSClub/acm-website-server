'use strict';

const zodOpenapi = require('@hono/zod-openapi');

const IdParamsSchema = zodOpenapi.z.object({
  id: zodOpenapi.z.coerce.number().openapi({
    param: {
      name: "id",
      in: "path"
    },
    required: ["id"],
    example: 42
  })
});

module.exports = IdParamsSchema;
