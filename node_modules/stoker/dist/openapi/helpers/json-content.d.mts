import { Z as ZodSchema } from '../../shared/stoker.a460570d.mjs';
import '@hono/zod-openapi';

declare function jsonContent<T extends ZodSchema>(schema: T, description: string): {
    content: {
        "application/json": {
            schema: T;
        };
    };
    description: string;
};

export { jsonContent as default };
