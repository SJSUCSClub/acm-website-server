import { NOT_FOUND as NOT_FOUND$1 } from '../http-status-codes.mjs';
import { NOT_FOUND } from '../http-status-phrases.mjs';

const notFound = (c) => {
  return c.json({
    message: `${NOT_FOUND} - ${c.req.path}`
  }, NOT_FOUND$1);
};

export { notFound as default };
