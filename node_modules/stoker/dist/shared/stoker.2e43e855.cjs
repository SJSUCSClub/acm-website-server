'use strict';

const middlewares_notFound = require('../middlewares/not-found.cjs');
const middlewares_onError = require('../middlewares/on-error.cjs');
const middlewares_serveEmojiFavicon = require('../middlewares/serve-emoji-favicon.cjs');

const index = {
	__proto__: null,
	notFound: middlewares_notFound,
	onError: middlewares_onError,
	serveEmojiFavicon: middlewares_serveEmojiFavicon
};

exports.index = index;
